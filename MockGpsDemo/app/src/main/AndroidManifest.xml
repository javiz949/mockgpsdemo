<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <!-- Legacy Xposed metadata is intentionally used for LSPatch compatibility. -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />

        <meta-data
            android:name="xposeddescription"
            android:value="Academic hook restricted to the MockGpsDemo thesis app" />

        <meta-data
            android:name="xposedminversion"
            android:value="82" />

        <!-- Defense in depth: the declared scope is ONLY the thesis demo app. -->
        <meta-data
            android:name="xposedscope"
            android:value="com.example.mockgpsdemo" />

    </application>

</manifest>
